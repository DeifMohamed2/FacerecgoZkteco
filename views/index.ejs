<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Attendance System</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <div class="nav-brand">
                <h1>📚 Attendance System</h1>
            </div>
            <ul class="nav-menu">
                <li><a href="/" class="nav-link active">Dashboard</a></li>
                <li><a href="/students" class="nav-link">Students</a></li>
                <li><a href="/students/add" class="nav-link">Add Student</a></li>
                <li><a href="/attendance" class="nav-link">Attendance</a></li>
                <li><a href="/devices" class="nav-link">Devices</a></li>
            </ul>
        </div>
    </nav>

    <main class="main-content">
        <div class="container">
            <div class="page-header">
                <h2>Dashboard</h2>
                <p>Overview of your attendance system</p>
            </div>

            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-icon">👥</div>
                    <div class="stat-info">
                        <h3><%= totalStudents %></h3>
                        <p>Total Students</p>
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon">✅</div>
                    <div class="stat-info">
                        <h3><%= todayAttendance %></h3>
                        <p>Today's Attendance</p>
                    </div>
                </div>
            </div>

            <div class="card">
                <div class="card-header">
                    <h3>Recent Attendance</h3>
                </div>
                <div class="card-body">
                    <% if (recentAttendance.length > 0) { %>
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>Student ID</th>
                                    <th>Student Name</th>
                                    <th>Date & Time</th>
                                    <th>Status</th>
                                    <th>Method</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% recentAttendance.forEach(record => { %>
                                    <tr>
                                        <td><span class="badge"><%= record.studentId %></span></td>
                                        <td><%= record.studentName %></td>
                                        <td><%= new Date(record.dateTime).toLocaleString() %></td>
                                        <td><span class="status-badge status-present"><%= record.status %></span></td>
                                        <td><%= record.verifyMethod %></td>
                                    </tr>
                                <% }); %>
                            </tbody>
                        </table>
                    <% } else { %>
                        <div class="empty-state">
                            <p>No attendance records yet</p>
                        </div>
                    <% } %>
                </div>
            </div>

            <div class="info-box">
                <h3>🔌 Device Setup Instructions</h3>
                <ol>
                    <li>Add students via the "Add Student" page</li>
                    <li>Note the 4-digit ID generated for each student</li>
                    <li>Manually add this ID to your face recognition device</li>
                    <li>Configure device: Menu → System → Device Type = "T&A PUSH"</li>
                    <li>Set Push Server IP to your server IP and Port to 8090</li>
                    <li>Attendance will be automatically recorded when students use the device</li>
                </ol>
            </div>
        </div>
    </main>

    <footer class="footer">
        <div class="container">
            <p>&copy; 2025 Attendance Management System. All rights reserved.</p>
        </div>
    </footer>
</body>
</html>

